- hosts: all
  remote_user: deep
  become: yes
  become_method: sudo
  tasks:
  - name: update 
    apt:
      update_cache: yes
  - name: Upgrade all packages to the latest version
    apt:
      name: "*"
      state: latest
  - name: Remove dependencies that are no longer required
    apt:
      autoremove: yes      
